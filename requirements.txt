# HiDRA — Python environment (tested with Python 3.12.3)
#
# GPU (recommended). Needs an NVIDIA GPU + driver >= 525 (CUDA 12+); tested on driver 580.
#   python3.12 -m venv hidra-env
#   source hidra-env/bin/activate
#   pip install -r requirements.txt
#
# CPU-only (works but MUCH slower): replace the jax line below with plain
#   jax==0.9.2
#
# The jax[cuda12] extra pulls the matching jaxlib + CUDA-12 plugin + NVIDIA cuda wheels,
# so no system-wide CUDA toolkit install is required.

jax[cuda12]==0.9.2
numpy==2.4.3
pandas==3.0.1
pyarrow==23.0.1

# Weight download from the Hugging Face Hub (python download_models.py).
huggingface_hub==1.30.0
